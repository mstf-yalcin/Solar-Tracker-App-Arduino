import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.may.solarcontroller',
  appName: 'solar-tracker-controller',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
